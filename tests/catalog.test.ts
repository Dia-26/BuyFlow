import { describe,it,expect } from "vitest";import { parseIntent,rank } from "../lib/catalog";
describe("deterministic buyer tools",()=>{it("extracts common budget/category/preference",()=>{expect(parseIntent("I need skincare under ₹1,000 with a good discount")).toMatchObject({category:"skin care",maxPrice:1000,preference:"high_discount"})});it("prioritizes a lower price for cheapest preference",()=>{const x:any=[{price:100,mrp:200,discountPercentage:50,sellerCount:1,stockStatus:"AVAILABLE",normalizedTitle:"one"},{price:200,mrp:300,discountPercentage:33,sellerCount:1,stockStatus:"AVAILABLE",normalizedTitle:"two"}];expect(rank(x,{preference:"cheapest"})[0].price).toBe(100)})});
