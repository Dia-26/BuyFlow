import { beforeEach,describe,expect,it } from "vitest";import { createHmac } from "crypto";import { toPaise,verifySignature,verifyWebhookSignature,webhookEventId } from "../lib/orders";
describe("Razorpay safety primitives",()=>{beforeEach(()=>{process.env.RAZORPAY_KEY_SECRET="test_secret";process.env.RAZORPAY_WEBHOOK_SECRET="webhook_secret"});it("converts only server amount to paise",()=>expect(toPaise(155.25)).toBe(15525));it("validates a genuine payment signature",()=>{const s=createHmac("sha256","test_secret").update("order_1|pay_1").digest("hex");expect(verifySignature("order_1","pay_1",s)).toBe(true);expect(verifySignature("order_1","pay_1","bad")).toBe(false)});it("validates webhook signatures and stable idempotency IDs",()=>{const body='{"event":"payment.captured"}',s=createHmac("sha256","webhook_secret").update(body).digest("hex");expect(verifyWebhookSignature(body,s)).toBe(true);expect(verifyWebhookSignature(body,"wrong")).toBe(false);expect(webhookEventId(body)).toBe(webhookEventId(body))})});
