import { NextRequest,NextResponse } from "next/server";import { proposeOrder } from "@/lib/orders";export async function POST(r:NextRequest){try{const {productId,upsellProductId,sessionId="demo-session",idempotencyKey}=await r.json();return NextResponse.json(await proposeOrder(productId,sessionId,idempotencyKey,upsellProductId));}catch(e){return NextResponse.json({error:(e as Error).message},{status:400})}}
