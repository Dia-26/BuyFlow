import { NextRequest,NextResponse } from "next/server";import { createPayment } from "@/lib/orders";export async function POST(r:NextRequest,{params}:{params:Promise<{id:string}>}){try{const b=await r.json();return NextResponse.json(await createPayment((await params).id,b.sessionId||"demo-session",b.simulateFailure));}catch(e){return NextResponse.json({error:(e as Error).message},{status:400})}}
