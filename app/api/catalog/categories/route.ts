import { NextResponse } from "next/server";import { db } from "@/lib/db";export async function GET(){const data=await db.product.groupBy({by:["category"],_count:true,orderBy:{category:"asc"}});return NextResponse.json(data);}
