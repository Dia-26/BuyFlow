import { NextRequest,NextResponse } from "next/server";import { db } from "@/lib/db";export async function GET(r:NextRequest){const ids=(r.nextUrl.searchParams.get("ids")||"").split(",").filter(Boolean).slice(0,3);const products=await db.product.findMany({where:{id:{in:ids}}});return NextResponse.json({products});}
