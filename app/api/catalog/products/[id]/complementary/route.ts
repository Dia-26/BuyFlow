import { NextResponse } from "next/server";import { db } from "@/lib/db";
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const base=await db.product.findUnique({where:{id:(await params).id}});if(!base)return NextResponse.json({error:"Not found"},{status:404});const related=await db.product.findFirst({where:{id:{not:base.id},stockStatus:"AVAILABLE",normalizedCategory:base.normalizedCategory,price:{lte:Math.max(base.price*.8,1)}},orderBy:[{discountPercentage:"desc"},{price:"asc"}]});return NextResponse.json({product:related,reason:"AI-suggested complementary product based on category similarity and availability."})}
